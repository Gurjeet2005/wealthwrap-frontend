[ ] All images must have alt text.
[ ] Color contrast ratios must meet WCAG AA (Primary Blue on White is safe).
[ ] Semantic HTML tags (main, nav, header) used throughout.
[ ] Forms have associated labels.
[ ] Interactive elements are keyboard accessible (tabindex, focus styles).
[X] ARIA roles used for specific widgets (modal, toast).
[X] Reduced motion media query respecting user preferences.
[X] "Skip to content" link (implied in standard semantic structure, added explicitly if needed).
